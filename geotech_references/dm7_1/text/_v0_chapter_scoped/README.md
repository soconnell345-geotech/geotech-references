# v0 chapter-scoped extracts (quarantined)

These two files were generated by the first-generation chapter-level
extraction pipeline (one LLM call per chapter, Claude Opus 4.6, ~650 s
per call, 32k output tokens). They are preserved for diffing against
the v1 section-level output but are NOT loaded by the retrieval layer.

**Do not treat as authoritative.** The live DM7 chapter text lives one
directory up in `dm7_1/text/chapter*.json`, produced by the v1 pipeline
(section-level chunking, Sonnet 4.6, tool_use structured output,
parallel extraction).

Generated: 2026-04-07
